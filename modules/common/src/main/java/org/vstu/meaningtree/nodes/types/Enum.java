package org.vstu.meaningtree.nodes.types;

import org.vstu.meaningtree.nodes.identifiers.Identifier;

public class Enum extends UserType {
    public Enum(Identifier name) {
        super(name);
    }
}
